import { FacebookLikesDirective } from './facebook-likes.directive';

describe('FacebookLikesDirective', () => {
  it('should create an instance', () => {
    const directive = new FacebookLikesDirective();
    expect(directive).toBeTruthy();
  });
});
